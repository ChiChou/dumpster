[project]
name = "dumpster"
version = "0.1.0"
description = "Decrypt IPA executables on jailbroken iOS devices"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools]
py-modules = ["main"]

[project.scripts]
dumpster = "main:main"
